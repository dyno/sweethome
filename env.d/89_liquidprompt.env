# git clone https://github.com/nojhan/liquidprompt.git

if [[ -n "${ENABLE_LIQUIDPROMPT}" ]]; then
  export LP_ENABLE_TIME=1
  [[ $- == *i* && -e "${HOME}/gitroot/liquidprompt/liquidprompt" ]] && source "${HOME}/gitroot/liquidprompt/liquidprompt"
fi
